# Phase 1 Day 1 — base requirements
# (No torch/transformers/peft/deepchem here. They live in requirements-deep.txt
# because deepchem in particular tends to conflict with Colab's pre-installed
# environment and silently breaks pip install when grouped with everything.)

# Cheminformatics
rdkit>=2024.3.1
pubchempy>=1.0.4

# Data
pandas>=2.0
polars>=0.20
pyarrow>=14
numpy>=1.26
openpyxl>=3.1
xlrd>=2.0
requests>=2.31
tqdm>=4.66

# Classical ML
scikit-learn>=1.3
scipy>=1.11

# Plotting
matplotlib>=3.8
seaborn>=0.13
adjustText>=1.3
